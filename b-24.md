# Configure NIS (Centralized Authentication)

sudo apt update
sudo apt install nis -y

sudo nano /etc/default/nis
# paste
NISCLIENT=false
NISSERVER=master
NISDOMAIN=lan.local

sudo domainname lan.local
sudo /usr/lib/yp/ypinit -m
# press ctrl+d and y



