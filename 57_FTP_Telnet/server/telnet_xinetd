# /etc/xinetd.d/telnet - Telnet Service Configuration

service telnet
{
    flags       = REUSE
    socket_type = stream
    wait        = no
    user        = root
    server      = /usr/sbin/in.telnetd
    log_on_failure += USERID
    disable     = no
    bind        = 0.0.0.0
}
